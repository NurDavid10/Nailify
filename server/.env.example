# Backend Environment Variables
# Copy this file to .env for local development
# In production (Railway), set these in the Variables dashboard

# Server Configuration
PORT=3001
NODE_ENV=development

# Database (PostgreSQL)
# Local: postgresql://user:password@localhost:5432/dbname
# Supabase: Get from Supabase Dashboard → Settings → Database
# Railway: Automatically provided when you add PostgreSQL service
DATABASE_URL=postgresql://nails-booking-postgres:nails-booking-postgres@localhost:5432/nails-booking-postgres

# JWT Configuration
# IMPORTANT: Generate a secure secret for production!
# Run: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d

# CORS Configuration
# Comma-separated list of allowed frontend origins
# Local: http://localhost:5173
# Production: https://your-app.vercel.app,https://yourdomain.com
CORS_ORIGIN=http://localhost:5173

# Default Admin Credentials (optional, for initial setup)
# Used by /api/admin/setup endpoint
DEFAULT_ADMIN_EMAIL=admin@nailsbooking.local
DEFAULT_ADMIN_PASSWORD=ChangeMe123!

# Twilio WhatsApp Configuration (for appointment reminders)
# Get these from: https://console.twilio.com
# Tutorial: https://www.twilio.com/docs/whatsapp/quickstart/node
TWILIO_ACCOUNT_SID=your_twilio_account_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token

# Twilio WhatsApp Number (Your Business Number)
TWILIO_WHATSAPP_NUMBER=whatsapp:+972546021965

# Cloudinary Configuration (for image uploads)
# Get these from: https://console.cloudinary.com/settings
CLOUDINARY_CLOUD_NAME=your_cloudinary_cloud_name
CLOUDINARY_API_KEY=your_cloudinary_api_key
CLOUDINARY_API_SECRET=your_cloudinary_api_secret
